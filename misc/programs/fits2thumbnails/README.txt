Use this script to create the thumbnail and fullsize jpeg images from
the FIT files. a web page containing an image gallery of a
list of images. The script takes these arguments in order:

1. input_dir = e.g."./images"
2. output_dir = e.g."./output"
3. (optional) filter list - only process files that match files in this file
